issues:
  exclude:
    - SA5011
  max-issues-per-linter: 0
  max-same-issues: 0
linters:
  enable:
    - errcheck
    - gofmt
    - gosimple
    - govet
    - ineffassign
    - misspell
    - staticcheck
    - testifylint
    - unused
linters-settings:
  testifylint:
    enable-all: true
    disable:
      - compares
      - empty
      - error-is-as
      - error-nil
      - expected-actual
      - float-compare
      - go-require
      - len
      - nil-compare
      - require-error
run:
  timeout: 50m

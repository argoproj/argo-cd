apiVersion: acme.cert-manager.io/v1
kind: Order
metadata:
  name: example-tls-order
  namespace: default
spec:
  request: LS0tLS1CRUdJTi==
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
status:
  state: errored
  reason: "Failed to retrieve the ACME order: 400 urn:ietf:params:acme:error:malformed"

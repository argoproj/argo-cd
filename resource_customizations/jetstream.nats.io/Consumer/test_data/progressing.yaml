apiVersion: jetstream.nats.io/v1beta2
kind: Consumer
metadata:
  name: consumer-name
spec:
  streamName: mystream
  durableName: my-pull-consumer
  deliverPolicy: all
  filterSubject: orders.received
  maxDeliver: 20
  ackPolicy: explicit

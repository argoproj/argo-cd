apiVersion: apps/v1beta2
kind: Deployment
metadata:
  name: guestbook-ui
spec:
  replicas: 1
  progressDeadlineSeconds: 3
  template:
    spec:
      containers:
        - name: guestbook-ui
          command: ["fail"]

apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: {{.Name}}
  namespace: {{.ArgoCDNamespace}}
spec:
  project: {{.Project}}
  source:
    repoURL: {{.RepoURL}}
    targetRevision: HEAD
    path: {{.Path}}/chart
    helm:
      valueFiles:
        - values.yaml
      externalValueFiles:
        - repoURL: {{.RepoURL}}
          targetRevision: HEAD
          valueFiles:
            - {{.Path}}/chart/values_external.yaml
        - repoURL: {{.RepoURL}}
          targetRevision: HEAD
          valueFiles:
            - {{.Path}}/chart/values_external_2.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: {{.DeploymentNamespace}}

apiVersion: v1
kind: Pod
metadata:
  name: my-pod
spec:
  containers:
    - name: main
      image: alpine:latest
      imagePullPolicy: IfNotPresent
      command:
        - "true"
  restartPolicy: Never

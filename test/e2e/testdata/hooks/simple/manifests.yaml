apiVersion: batch/v1
kind: Job
metadata:
  name: hook-1
  annotations:
    argocd.argoproj.io/hook: Sync
spec:
  template:     
    spec:
      containers:
      - name: noop
        image: alpine
        command: ["true"]

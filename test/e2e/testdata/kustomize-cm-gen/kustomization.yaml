configMapGenerator:
  - name: my-map
    literals:
      - foo=bar
generatorOptions:
  annotations:
    argocd.argoproj.io/compare-options: IgnoreNeedsPruning
    argocd.argoproj.io/sync-options: NoPrune
kind: Kustomization

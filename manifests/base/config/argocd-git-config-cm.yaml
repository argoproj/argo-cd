apiVersion: v1
kind: ConfigMap
metadata:
  name: argocd-git-config-cm
  labels:
    app.kubernetes.io/name: argocd-git-config-cm
    app.kubernetes.io/part-of: argocd
data:
  .gitconfig: |
    # This is a placeholder for your custom git configuration.
    # For example, to mark all repositories as safe:
    # [safe]
    #   directory = *
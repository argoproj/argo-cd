# argocd-default-ca-cm.yaml example

An example of an `argocd-default-ca-cm.yaml` file for configuring a default CA bundle for cluster connections:

```yaml
{!docs/operator-manual/argocd-default-ca-cm.yaml!}
```

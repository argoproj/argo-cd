apiVersion: argoproj.io/v1alpha1
kind: ConfigManagementPlugin
metadata:
  name: ksonnet
spec:
  init:
    command: [ks, version]
  generate:
    command: [sh, -c, "ks show $ARGOCD_APP_ENV"]
  discover:
    find:
      glob: "**/*/main.jsonnet"
